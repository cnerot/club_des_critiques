{% extends 'base.html.twig' %}

{% block title %}
   Envoyer un message
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            {{ form_start(form) }}
                {{ form_rest(form) }}
                <div class="right">
                    <button class="btn-large" type="submit">Envoyer</button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
